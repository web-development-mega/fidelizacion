APP_NAME="Fidelizacion"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://comunicaciones.megatecnicentro.com

# Base de datos (HostGator suele usar 'localhost' como host de MySQL)
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=megatecn_laravel_DB
DB_USERNAME=megatecn_laravel
DB_PASSWORD="4=[Rw~auPZ3i"

# Sesiones / caché / colas (según tu plantilla del repo)
SESSION_DRIVER=database
SESSION_LIFETIME=120
QUEUE_CONNECTION=database
CACHE_STORE=database

# Redis (si no lo usas, déjalo así)
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Correo (rellena si vas a enviar emails)
MAIL_MAILER=smtp
MAIL_HOST=smtp.tu_proveedor.com
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=no-reply@megatecnicentro.com
MAIL_FROM_NAME="Fidelizacion"

# Archivos / S3 (opcional)
FILESYSTEM_DISK=local

VITE_APP_NAME="${APP_NAME}"

